# Plan: Add Loading Screen to SETUP Filters

## Goal
After pressing any filter button (tier, class, nation) in the SETUP section (tank stats), immediately show button press feedback, then display a loading overlay with WoT logo, text "ІНФОРМАЦІЯ ЗАВАНТАЖУЄТЬСЯ" (localized), and an indeterminate progress bar while the tank grid refreshes.

## Discoveries
- SETUP UI is in stats_ai.py, with toggle functions: toggle_tier_filter, toggle_class_filter, toggle_nation_filter.
- These functions update button visuals and call refresh_ai_view() synchronously.
- Main app has splash screen implementation in show_small_loading_splash() with progress bar animation.
- Logo available as self.main_app.logo_splash.
- UI uses Tkinter, with ai_frame containing filters and ai_grid_container for tank grid.
- Localization via self.locale_manager, text should be translated.

## Relevant Files
- **stats_ai.py**: Contains filter toggle methods, build_ai_ui for UI setup, refresh_ai_view for grid update.
- **main.py**: Has splash screen code for reference (show_small_loading_splash, update_startup_progress).

## Implementation Notes
- Add loading_frame to ai_frame in build_ai_ui: tk.Frame with canvas for logo/text/progress.
- Use ttk.Progressbar in indeterminate mode for loading animation.
- Modify toggle functions: update button colors first, then pack loading_frame, start progress, call refresh_ai_view (synchronous for now), pack_forget loading_frame, stop progress.
- Since refresh is fast with debounce, loading may be brief; consider artificial delay if needed for UX.
- Text: self.t("ui", "loading_info") or hardcoded "ІНФОРМАЦІЯ ЗАВАНТАЖУЄТЬСЯ".
- Ensure loading_frame covers ai_grid_container properly.
- Test with large datasets; if refresh is slow, consider threading refresh and updating progress.</content>
<parameter name="filePath">D:\!WORK\WOT\WOTtraner\WORK\SETUP S MAPS WoT Assistant_1.00\.kilo\plans\1776981543028-brave-knight.md