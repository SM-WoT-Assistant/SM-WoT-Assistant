"""
═══════════════════════════════════════════════════════════════════════════════
ИНСТРУКЦИЯ: АВТОМАТИЧЕСКОЕ ОБНОВЛЕНИЕ TTH ПРИ ИЗМЕНЕНИИ ВЕРСИИ КЛИЕНТА
═══════════════════════════════════════════════════════════════════════════════

НАЗНАЧЕНИЕ:
-----------
Система автоматически обнаруживает обновление клиента WoT и обновляет базу
технических характеристик (tank_tth.json) БЕЗ необходимости ручного запуска
Orion. Декодирование выполняется только для ИЗМЕНЕННЫХ файлов.


КАК ЭТО РАБОТАЕТ:
-----------------

1. ОБНАРУЖЕНИЕ ИЗМЕНЕНИЯ
   └─ При запуске программы проверяется version.xml в папке игры
   └─ Сравнивается с game_version в settings.json
   └─ Если версия изменилась → запускается обновление

2. ОПРЕДЕЛЕНИЕ ИЗМЕНЕННЫХ ФАЙЛОВ
   └─ Используется manifest-система (.tank_extract_manifest.json)
   └─ Каждый файл отмечается: размер, CRC, дата修禁
   └─ Только измененные файлы подлежат обработке (экономия времени)

3. ДЕКОДИРОВАНИЕ (если нужно)
   └─ Orion запускается в СКРЫТОМ режиме (CREATE_NO_WINDOW)
   └─ Файлы обрабатываются БАТЧАМИ (по 24 шт сразу)
   └─ Процесс работает в фоне, без блокирования UI
   └─ Автоматические повторы при таймауте

4. ОБНОВЛЕНИЕ БАЗ ДАННЫХ
   └─ Новые значения TTH читаются из декодированных XML
   └─ tank_tth.json обновляется БЕЗОПАСНО (без изменения tank_db.json)
   └─ Логируется coverage (сколько танков имеют TTH данные)


ТЕСТИРОВАНИЕ:
--------------

╔═══════════════════════════════════════════════════════════════════════════╗
║ СПОСОБ 1: Быстрое тестирование (с мокированием версии)                  ║
╚═══════════════════════════════════════════════════════════════════════════╝

  1. Откройте PowerShell или терминал в папке проекта
  
  2. Запустите тест-скрипт:
     python test_auto_update.py v.2.3.0.0

  3. Скрипт покажет:
     ✓ Текущую версию в settings.json
     ✓ Новую версию (v.2.3.0.0)
     ✓ Количество XML файлов по странам
     ✓ Статус tank_db.json и tank_tth.json
     ✓ Что произойдет при запуске программы

  4. Закройте программу (если она запущена)
  
  5. Запустите программу заново:
     python main.py
     
  6. Наблюдайте в консоли логи:
     [EXTRACTION] Витягую метадані з scripts.pkg...
     [EXTRACTION] Змінені XML техніки: XXX
     [EXTRACTION] Зміни по країнах:
       USA: X файлів (...)
       USSR: X файлів (...)
       ...
     [DATABASE] Оновлюю tank_tth.json...
     [DECODER] Помічено XXX файлів для батч-декодування
     [DECODER] Тихий батч-декод Orion для XXX XML...
     [SUCCESS] TTH merge завершено: parsed=X, updated=Y, total=Z

  7. Проверьте результаты:
     ✓ Окно Orion НЕ должно появляться (скрытый режим)
     ✓ tank_tth.json должен обновиться с новыми значениями
     ✓ В AI STATS увидите обновленные TTH (reload, turret_armor, view_range)


╔═══════════════════════════════════════════════════════════════════════════╗
║ СПОСОБ 2: Тестирование с реальным обновлением клиента                   ║
╚═══════════════════════════════════════════════════════════════════════════╝

  1. Если выходит реальное обновление WoT:
     └─ Просто запустите программу
     └─ Обновление будет обнаружено автоматически
     └─ TTH обновится в фоне без вмешательства

  2. Если хотите вернуть старую версию для repeat-теста:
     └─ Откройте settings.json
     └─ Измените "game_version": "v.X.X.X.X" на старое значение
     └─ Запустите python test_auto_update.py v.X.X.X.X снова


╔═══════════════════════════════════════════════════════════════════════════╗
║ СПОСОБ 3: Проверка покрытия TTH без полного обновления                  ║
╚═══════════════════════════════════════════════════════════════════════════╝

  Просмотрите концевые логи из предыдущих запусков:
  
  python test_auto_update.py
  (без указания версии - только анализ текущего состояния)
  
  Получите статистику:
  - Сколько XML файлов в каждой стране
  - Размер и версию tank_db.json и tank_tth.json
  - Процент покрытия TTH (скольким танкам доступны данные)


КОНФИГУРАЦИЯ:
--------------

Параметры в settings.json для управления поведением:

  "pause_tank_auto_rebuild": true
    └─ true  = стабильный режим (обновляет только TTH, не трогает tank_db)
    └─ false = полный rebuild (переписывает tank_db и tank_tth)

  "tth_decode_retry_enabled": true
    └─ true  = при parse=0 автоматически запускает Orion
    └─ false = пропускает автодекод (используется старый tank_tth.json)


ЛОГИРОВАНИЕ И ДИАГНОСТИКА:
----------------------------

Все логи автообновления выводятся в консоль:

  [EXTRACTION] - этап извлечения XML из game packages
  [DATABASE]   - этап парсинга и обновления TTH
  [DECODER]    - этап декодирования с Orion
  [SUCCESS]    - успешное завершение этапа
  [ERROR]      - критическая ошибка
  [WARN]       - предупреждение (но процесс продолжает работу)

Если что-то пошло не так:
  1. Проверьте путь к игре в settings.json (wot_path)
  2. Убедитесь, что WoT не запущена во время обновления
  3. Проверьте наличие PjOrion.exe в tools/orion/
  4. Посмотрите логи на предмет "Permission denied" или таймаутов


ОПТИМИЗАЦИИ:
-------------

✓ Файлы сравниваются по fingerprint (size + CRC)
  └─ Если файл не изменился → пропускается

✓ Декодирование батчами по 24 файла
  └─ Уменьшает нагрузку на Orion
  └─ Избегает REPL-режима (когда Orion зависает)

✓ Таймауты с автоматическими убийств Orion.exe
  └─ taskkill /f /im PjOrion.exe
  └─ Защита от зависаний

✓ Логирование по странам
  └─ Видно, какие страны обновились
  └─ Помощь в диагностике проблем


ПРИМЕЧАНИЯ:
-----------

• Первый запуск может занять 2-5 минут (зависит от кол-ва измененных файлов)
• Последующие запуски без изменений версии - мгновенны
• Величина batch-а (24 файла) оптимальна для большинства PC
  └─ Если Orion зависает, уменьшите в tank_extractor.py: batch_size=12

• Если Orion все еще показывает окно несмотря на флаги:
  └─ Используется fallback: powershell Start-Process -WindowStyle Hidden
  └─ Это будет добавлено в следующем обновлении

═══════════════════════════════════════════════════════════════════════════════
"""

# Простой скрипт для отладки
if __name__ == "__main__":
    import os
    doc = __doc__
    print(doc)
    
    # Интерактивная проверка
    print("\nБыстрая проверка состояния:")
    print("-" * 70)
    
    settings_path = "settings.json"
    if os.path.exists(settings_path):
        import json
        with open(settings_path, 'r', encoding='utf-8') as f:
            s = json.load(f)
        print(f"✓ game_version: {s.get('game_version', 'not set')}")
        print(f"✓ wot_path: {s.get('wot_path', 'not set')}")
        print(f"✓ tth_schema_version: {s.get('tth_schema_version', 0)}")
        print(f"✓ pause_tank_auto_rebuild: {s.get('pause_tank_auto_rebuild', True)}")
        print(f"✓ tth_decode_retry_enabled: {s.get('tth_decode_retry_enabled', True)}")
    else:
        print("✗ settings.json не найден!")
