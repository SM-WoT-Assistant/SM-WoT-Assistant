DEPRECATED 2026-09-02: tank_slots_db.json is an orphan — 0 importers in *.py, *.spec, build.py. Not in config.CRITICAL_BUNDLE_FILES, not in build_admin.py --add-data, not in config.DEFAULT_FILES. Superseded by tank_slots_full.json (which is itself being refreshed by the Bug 3.z.D+admin_build_generator pipeline). Plan to delete in v1.0.74 after 30-day observation window (02.10.2026). See AGENTS.md "Soft-delete (Корзина)" for the full process.
